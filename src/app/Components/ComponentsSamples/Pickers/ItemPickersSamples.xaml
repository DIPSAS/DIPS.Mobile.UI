<?xml
    version="1.0"
    encoding="utf-8"?>

<dui:ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:dui="http://dips.com/mobile.ui"
                 xmlns:pickers="clr-namespace:Components.ComponentsSamples.Pickers"
                 xmlns:sampleData="clr-namespace:Components.SampleData"
                 x:Class="Components.ComponentsSamples.Pickers.ItemPickersSamples"
                 Title="Item pickers">
    <dui:ContentPage.BindingContext>
        <pickers:ItemPickersSamplesViewModel />
    </dui:ContentPage.BindingContext>
    <dui:VerticalStackLayout>
        <dui:Label Text="ItemPicker, Mode: Context Menu"
                   Margin="5" />
        <dui:ItemPicker VerticalOptions="Start"
                        Placeholder="Person"
                        HorizontalOptions="Start"
                        Mode="ContextMenu"
                        ItemsSource="{Binding People}"
                        ItemDisplayProperty="DisplayName" />

        <dui:Label Text="ItemPicker, Mode: Bottom Sheet"
                   Margin="5" />
        <dui:ItemPicker VerticalOptions="Start"
                        Placeholder="Person"
                        HorizontalOptions="Start"
                        Mode="BottomSheet"
                        SelectedItem="{Binding SelectedPerson}"
                        ItemsSource="{Binding People}"
                        ItemDisplayProperty="DisplayName" />


        <dui:ListItem Title="Contact">
            <dui:ItemPicker VerticalOptions="Start"
                            Placeholder="Person"
                            HorizontalOptions="Start"
                            Mode="BottomSheet"
                            ItemsSource="{Binding Contacts}"
                            ItemDisplayProperty="ShortDisplayName">
                <dui:ItemPicker.BottomSheetConfiguration>
                    <dui:BottomSheetConfiguration HasSearchBar="False">
                        <dui:BottomSheetConfiguration.SelectableItemTemplate>
                            <ControlTemplate>
                                <dui:Label Padding="20"
                                           Text="{TemplateBinding Item.DisplayName}"
                                           BackgroundColor="{TemplateBinding IsSelected, Converter={dui:BoolToObjectConverter TrueObject={dui:Colors color_obsolete_success}, FalseObject={dui:Colors color_obsolete_danger}}}" />
                            </ControlTemplate>
                        </dui:BottomSheetConfiguration.SelectableItemTemplate>
                    </dui:BottomSheetConfiguration>
                </dui:ItemPicker.BottomSheetConfiguration>
            </dui:ItemPicker></dui:ListItem>
    </dui:VerticalStackLayout>
</dui:ContentPage>