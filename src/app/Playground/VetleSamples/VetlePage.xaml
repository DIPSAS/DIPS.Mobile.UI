<?xml
    version="1.0"
    encoding="utf-8"?>

<dui:ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:vetleSamples="clr-namespace:Playground.VetleSamples"
                 xmlns:dui="http://dips.com/mobile.ui"
                 x:Class="Playground.VetleSamples.VetlePage"
                 x:Name="This"
                 HideSoftInputOnTapped="True">

    <dui:ContentPage.BindingContext>
        <vetleSamples:VetlePageViewModel />
    </dui:ContentPage.BindingContext>

    <CollectionView ItemsSource="{Binding TestStrings}">
        
        <CollectionView.FooterTemplate>
            <DataTemplate>
                <dui:ListItem Command="{Binding Navigate}"
                              Title="h"></dui:ListItem>
            </DataTemplate>
        </CollectionView.FooterTemplate>
        
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <dui:ListItem Title="{Binding .}"
                              Margin="{dui:Thickness Left=size_3, Right=size_3}" />
            </DataTemplate>
        </CollectionView.ItemTemplate>
        
    </CollectionView>


</dui:ContentPage>