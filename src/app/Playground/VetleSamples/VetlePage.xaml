<?xml
    version="1.0"
    encoding="utf-8"?>

<dui:ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:vetleSamples="clr-namespace:Playground.VetleSamples"
                 xmlns:dui="http://dips.com/mobile.ui"
                 xmlns:system="clr-namespace:System;assembly=System.Runtime"
                 xmlns:divider="clr-namespace:DIPS.Mobile.UI.Effects.Layout.Divider;assembly=DIPS.Mobile.UI"
                 x:Class="Playground.VetleSamples.VetlePage"
                 x:Name="This"
                 HideSoftInputOnTapped="True"
                 BackgroundColor="{dui:Colors color_neutral_10}">
    <dui:ContentPage.BindingContext>
        <vetleSamples:VetlePageViewModel />
    </dui:ContentPage.BindingContext>

    <dui:ContentPage.Resources>
        <vetleSamples:TemplateSelector x:Key="TemplateSelector" />
    </dui:ContentPage.Resources>

    <dui:RefreshView Command="{Binding RefreshCommand}"
                     IsRefreshing="{Binding IsRefreshing}">
    <dui:CollectionView2 ItemsSource="{Binding TestObject2s}"
                            x:Name="CollectionView"
                            IsGrouped="False"
                            BackgroundColor="Transparent"
                            dui:Layout.AutoCornerRadius="False"
                            dui:Layout.AutoDivider="True">
        
        <!--<dui:Layout.AutoDivider>
            <divider:DividerConfigurator Padding="{dui:Thickness Top=size_4}"
                                         Margin="{dui:Thickness Left=size_2}" />
        </dui:Layout.AutoDivider>-->
        
        <!--<dui:CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Horizontal"></LinearItemsLayout>
        </dui:CollectionView.ItemsLayout>-->
        
        <dui:CollectionView2.GroupHeaderTemplate>
            <DataTemplate>
                <Grid Padding="8">
                    <dui:ListItem Title="he"></dui:ListItem>
                </Grid>
            </DataTemplate>
        </dui:CollectionView2.GroupHeaderTemplate>
        
        <!--<dui:CollectionView.Header>
            <Button Text="Test!"
                    BackgroundColor="Red"
                    Clicked="Button_OnClicked"></Button>
        </dui:CollectionView.Header>-->
        
        <dui:CollectionView2.Header>
            <Grid Padding="8">
            <dui:ListItem Title="Hello"></dui:ListItem>
            </Grid>
        </dui:CollectionView2.Header>
        
        <dui:CollectionView2.Footer>
            <Grid Padding="8">
                
            <dui:ListItem Title="footer"></dui:ListItem>
            </Grid>
        </dui:CollectionView2.Footer>
        
        <dui:CollectionView2.GroupFooterTemplate>
            <DataTemplate>
                <Grid Padding="8">
                    <dui:ListItem Title="footer"></dui:ListItem>
                </Grid>
            </DataTemplate>
        </dui:CollectionView2.GroupFooterTemplate>
        
        <dui:CollectionView2.ItemTemplate>
            <DataTemplate x:DataType="system:String">
                <dui:ListItem Title="Hei" />
            </DataTemplate>
        </dui:CollectionView2.ItemTemplate>

    </dui:CollectionView2>
    </dui:RefreshView>

</dui:ContentPage>