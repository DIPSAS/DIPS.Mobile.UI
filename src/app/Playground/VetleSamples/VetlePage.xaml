<?xml
    version="1.0"
    encoding="utf-8"?>

<dui:ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:vetleSamples="clr-namespace:Playground.VetleSamples"
                 xmlns:dui="http://dips.com/mobile.ui"
                 x:Class="Playground.VetleSamples.VetlePage"
                 x:DataType="{x:Type vetleSamples:VetlePageViewModel}"
                 Padding="20"
                 x:Name="This"
                 HideSoftInputOnTapped="True">

    <dui:ContentPage.BindingContext>
        <vetleSamples:VetlePageViewModel />
    </dui:ContentPage.BindingContext>

    <dui:ContentPage.ToolbarItems>
        
        <dui:ContextMenuToolbarItem Text="Context">
            <dui:ContextMenuToolbarItem.ContextMenu>
                <dui:ContextMenu Title="Hei" />
            </dui:ContextMenuToolbarItem.ContextMenu>
        </dui:ContextMenuToolbarItem>


    </dui:ContentPage.ToolbarItems>
   
    <VerticalStackLayout>
        <dui:Button 
                ImageSource="{dui:Icons alert_line}"
                Padding="0"
                HeightRequest="{dui:Sizes size_14}"
                WidthRequest="{dui:Sizes size_14}"
                CornerRadius="{dui:Sizes size_0}"/>
        
        <dui:Button ImageSource="{dui:Icons arrow_back_line}"
                    Style="{dui:Styles Button=PrimaryIconButtonLarge}"/>
        
    </VerticalStackLayout>
    
    
   
    
</dui:ContentPage>