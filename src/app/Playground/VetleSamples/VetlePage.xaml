<?xml
    version="1.0"
    encoding="utf-8"?>

<dui:ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:vetleSamples="clr-namespace:Playground.VetleSamples"
                 xmlns:iOsSpecific="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
                 xmlns:system="clr-namespace:System;assembly=System.Runtime"
                 xmlns:loadingOverlay="clr-namespace:DIPS.Mobile.UI.Components.Loading.LoadingOverlay;assembly=DIPS.Mobile.UI"
                 xmlns:delayedView="clr-namespace:DIPS.Mobile.UI.Components.Loading.DelayedView;assembly=DIPS.Mobile.UI"
                 xmlns:gallery="clr-namespace:DIPS.Mobile.UI.Components.Gallery;assembly=DIPS.Mobile.UI"
                 xmlns:dui="http://dips.com/mobile.ui"
                 x:Class="Playground.VetleSamples.VetlePage"
                 x:Name="This"
                 HideSoftInputOnTapped="True"
                 Title="Hello"
                 SafeAreaEdges="None"
                 Shell.NavBarIsVisible="True">


    <dui:ContentPage.BindingContext>
        <vetleSamples:VetlePageViewModel />
    </dui:ContentPage.BindingContext>

    <dui:ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="{dui:Icons alert_line}"></ToolbarItem>
        <ToolbarItem Text="Hello"></ToolbarItem>
    </dui:ContentPage.ToolbarItems>

    <VerticalStackLayout BindableLayout.ItemsSource="{Binding ChipGroupTestViewModels}">
        
        <BindableLayout.ItemTemplate>
            <DataTemplate x:DataType="{x:Type vetleSamples:ChipGroupTestViewModel}">
                
                <dui:ChipGroup ItemsSource="{Binding ItemsSource}"
                               SelectedItems="{Binding SelectedItems, Mode=TwoWay}"
                               SelectionMode="Single"
                               ItemDisplayProperty="Name" />
                
            </DataTemplate>
        </BindableLayout.ItemTemplate>
       
        
    </VerticalStackLayout>

</dui:ContentPage>